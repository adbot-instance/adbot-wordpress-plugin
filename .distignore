# Legacy note: release zips are built with `dev/tools/build-release.sh`, which
# stages from `adbot/` using `.plugin-build-excludes`. This file is kept for
# any tooling that still references `.distignore`.
