{
    "name": "adbot/adbot-wordpress-plugin",
    "description": "Adbot WordPress plugin — connect your Google marketing stack in one click.",
    "type": "wordpress-plugin",
    "license": "GPL-2.0-or-later",
    "require": {
        "php": ">=8.0"
    },
    "autoload": {
        "psr-4": {
            "Adbot\\": "includes/"
        }
    },
    "config": {
        "optimize-autoloader": true,
        "sort-packages": true
    }
}
